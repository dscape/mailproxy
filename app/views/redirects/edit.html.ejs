<div class="hero-unit">
  <form id="redirect-form" class="form-horizontal" action="/redirects/<%= params.id %>?_method=PUT" method="POST">
    <fieldset>
      <legend>Update this Redirect</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {redirect: redirect}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Save', {type: 'submit', class: 'btn btn-primary'}) %>
        <%- contentTag('button', 'Remove', {type: 'submit', formaction: '/redirects/' + params.id + '?_method=DELETE', formmethod: 'POST', class: 'btn btn-danger'}) %>
        <a href="/redirects" class="btn">Back to main</a>
      </div>
    </fieldset>
  </form>
</div>
