<div class="hero-unit">
  <form id="redirect-form" class="form-horizontal" action="/redirects" method="POST">
    <fieldset>
      <legend>Create a new Redirect</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {redirect: {}}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
        <a href="/redirects" class="btn">Back to main</a>
      </div>
    </fieldset>
  </form>
</div>
